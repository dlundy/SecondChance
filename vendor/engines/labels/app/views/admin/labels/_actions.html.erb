<ul>
  <% if Admin::LabelsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_labels_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_label_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::LabelsController.sortable? and Label.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Labels"),
                 admin_labels_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Labels"),
                 admin_labels_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>