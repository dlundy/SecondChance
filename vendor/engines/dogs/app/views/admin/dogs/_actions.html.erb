<ul>
  <% if Admin::DogsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_dogs_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_dog_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::DogsController.sortable? and Dog.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Dogs"),
                 admin_dogs_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Dogs"),
                 admin_dogs_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>